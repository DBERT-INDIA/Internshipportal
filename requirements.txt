flask>=2.3.0
werkzeug>=2.3.0
gunicorn>=21.0.0
python-dotenv>=1.0.0
requests>=2.31.0
PyJWT>=2.8.0
# Required only when EMAIL_PROVIDER=ses (Amazon SES HTTPS API):
boto3>=1.34.0
# Used only by migrate_legacy_users.py / xlsx import (not required by app.py at runtime):
pandas>=2.0.0
openpyxl>=3.1.0
# Track 2 §6: HTML→PDF for the CV builder. xhtml2pdf (reportlab-based) chosen for
# minimal system deps over weasyprint (which needs GTK/cairo native libs). The PDF
# route degrades to HTML if this import is unavailable, so it's a soft dependency.
xhtml2pdf>=0.2.16

# Phase 7 — Fernet encryption for ambassador UPI ids (spec §5.4)
cryptography>=42.0.0
